function multiplication(n) {
  for (let i = 1; i <= 10; i++) {
    console.log(`${n} × ${i} = ${n * i}`);
  }
}

setTimeout(multiplication, 2000, 5);
function multiplication(n) {
  for (let i = 1; i <= 10; i++) {
    console.log(`${n} × ${i} = ${n * i}`);
  }
}

setTimeout(multiplication, 2000, 5);